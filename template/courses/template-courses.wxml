<template name="courses">
  <view class="courses" style="background-color:{{color}}">
    <view>
      <text class="title">{{title}}</text>
    </view>
    <view>
      <text class="subtitle">{{subtitle}}</text>
    </view>
  </view>
</template>